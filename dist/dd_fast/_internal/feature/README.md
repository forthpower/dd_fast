# 功能模块开发指南

本目录包含dd_fast应用的所有功能模块，采用统一的模块化架构，便于添加新功能。

## 目录结构

```
feature/
├── file_duplicator/                    # 文件复制器功能
│   ├── file_duplicator.py             # 功能实现
│   ├── file_duplicator_manager.py     # 主界面对接管理器
│   └── button_config.py               # 按钮配置
├── workflow/                          # Workflow编辑器功能
│   ├── workflow_manager.py            # 主界面对接管理器
│   ├── start.py                       # 独立启动脚本
│   ├── start.bat                      # Windows启动脚本
│   ├── start.sh                       # Linux/Mac启动脚本
│   ├── workflow-explorer.html         # 可视化编辑器
│   ├── workflow-explorer.js           # 核心逻辑
│   └── explorer-demo.html             # 演示页面
├── schema_generator/                  # Schema生成器功能
│   ├── schema_generator_manager.py    # 主界面对接管理器
│   ├── app.py                         # Flask应用主文件
│   ├── start_server.sh                # 独立启动脚本
│   ├── static/                        # 前端资源
│   │   ├── index.html                 # 主页面
│   │   └── script.js                  # JavaScript逻辑
│   ├── backend/                       # 后端模块
│   │   ├── api/                       # API接口
│   │   ├── database/                  # 数据库管理
│   │   ├── parser/                    # 解析器
│   │   └── utils/                     # 工具函数
│   └── requirements.txt               # Python依赖
├── command_tool/                      # 命令大全功能
│   ├── command_tool.py                # 功能实现
│   └── command_tool_manager.py        # 主界面对接管理器
└── README.md                          # 本文件
```

## 功能模块说明

### 1. 文件复制器 (file_duplicator)
- **功能**: 多语言文件复制工具
- **技术**: Tkinter桌面应用
- **端口**: 无（桌面应用）
- **按钮颜色**: 蓝色 (#3498db)

### 2. Workflow编辑器 (workflow)
- **功能**: Primer Workflow可视化编辑器
- **技术**: HTML + JavaScript + Python HTTP服务器
- **端口**: 8000
- **按钮颜色**: 橙色 (#FF6B35)

### 3. Schema生成器 (schema_generator)
- **功能**: 可视化的Schema生成器
- **技术**: Flask + SQLite + HTML/JavaScript
- **端口**: 5010
- **按钮颜色**: 紫色 (#8E44AD)

### 4. 命令大全 (command_tool)
- **功能**: 终端命令顺序执行工具
- **技术**: Tkinter桌面应用
- **端口**: 无（桌面应用）
- **按钮颜色**: 橙色 (#E67E22)

## 添加新功能的步骤

### 1. 创建功能文件夹
```bash
mkdir feature/your_feature_name
```

### 2. 创建管理器文件
创建 `your_feature_name_manager.py`，实现以下接口：

```python
class YourFeatureManager:
    def __init__(self):
        # 初始化代码
        pass
    
    def get_button_config(self):
        """返回按钮配置"""
        return {
            "text": "🎯 你的功能名称",
            "font": ("Arial", 20, "bold"),
            "bg": "#颜色代码",
            "fg": "white",  # 或 "black"
            "relief": "flat",
            "width": 25,
            "height": 3,
            "cursor": "hand2",
            "bd": 0,
            "highlightthickness": 0
        }
    
    def get_hover_colors(self):
        """返回悬停颜色 (原始颜色, 悬停颜色)"""
        return "#原始颜色", "#悬停颜色"
    
    def open_feature(self):
        """打开功能"""
        # 启动你的功能
        pass
    
    def cleanup(self):
        """清理资源"""
        # 清理代码
        pass
```

### 3. 在主应用中集成
在 `app.py` 中：

1. 导入管理器：
```python
from feature.your_feature_name.your_feature_name_manager import YourFeatureManager
```

2. 初始化管理器：
```python
def __init__(self):
    # 其他管理器...
    self.your_feature_manager = YourFeatureManager()
```

3. 创建按钮方法：
```python
def create_your_feature_button(self, parent):
    # 参考其他功能的实现
    pass

def open_your_feature(self):
    self.your_feature_manager.open_feature()
```

4. 在 `create_buttons` 中调用：
```python
self.create_your_feature_button(button_container)
```

5. 在 `on_closing` 中清理：
```python
self.your_feature_manager.cleanup()
```

## 管理器接口规范

所有功能管理器必须实现以下接口：

### 必需方法
- `get_button_config()`: 返回按钮配置字典
- `get_hover_colors()`: 返回悬停颜色元组
- `open_feature()`: 打开功能的主方法
- `cleanup()`: 清理资源的方法

### 按钮配置规范
```python
{
    "text": "按钮文本",
    "font": ("字体", 大小, "样式"),
    "bg": "背景颜色",
    "fg": "文字颜色",
    "relief": "边框样式",
    "width": 宽度,
    "height": 高度,
    "cursor": "鼠标样式",
    "bd": 边框宽度,
    "highlightthickness": 高亮宽度
}
```

### 颜色建议
- 蓝色系: #3498db, #2980b9 (文件操作)
- 橙色系: #FF6B35, #E55A2B (工作流)
- 紫色系: #8E44AD, #7D3C98 (数据生成)
- 绿色系: #27AE60, #229954 (分析工具)
- 红色系: #E74C3C, #C0392B (监控工具)

## 注意事项

1. **端口管理**: 确保不同功能使用不同端口，避免冲突
2. **资源清理**: 在 `cleanup()` 方法中正确关闭服务器和进程
3. **错误处理**: 在 `open_feature()` 中添加适当的错误处理
4. **依赖检查**: 对于需要额外依赖的功能，在启动前检查依赖是否安装
5. **文件检查**: 检查必要文件是否存在，提供友好的错误提示

## 开发建议

1. **模块化**: 将功能逻辑和管理器分离
2. **配置化**: 将按钮样式、颜色等配置化
3. **文档化**: 为每个功能添加README说明
4. **测试**: 确保功能可以独立启动和关闭
5. **兼容性**: 考虑不同操作系统的兼容性
